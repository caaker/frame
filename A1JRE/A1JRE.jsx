import React                        from         'react';
import { connect, useSelector }     from         'react-redux';

export default () => {
  console.logD('DEBUG: L3 : F1-Page-Test ');
  return (
    <div id="page-test"></div>
  );
};